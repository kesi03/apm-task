{
  "name": "@mockholm/ci-apm-trace",
  "version": "1.0.0",
  "description": "Sends CI pipeline traces to Elastic APM",
  "main": "dist/lifecycle.js",
  "bin": {
    "ci-apm-trace": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^5.0.0",
    "elastic-apm-node": "^4.13.0",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/yargs": "^17.0.32",
    "typescript": "^5.5.0"
  }
}
